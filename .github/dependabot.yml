version: 2

# GitHub Actions only. Dependabot's "npm" ecosystem also covers pnpm lockfiles, but
# enabling it here would put Next.js, React and Tailwind major versions into the
# update queue unattended; that is the owner's call.
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
