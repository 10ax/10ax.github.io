version: 2

# GitHub Actions only. Bumping the npm ecosystem here would put Next.js, React and
# Tailwind major versions into the update queue unattended; that is the owner's call.
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
